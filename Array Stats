#include <iostream>
#include <cstdlib>
#include <ctime>
#include <cmath>

using namespace std;

const int maxBarSize = 60;
const int ciel(956/60);

int minimum(int *v, int r);
int maximum(int *v, int r);

int random() {
    int i;
    cin >> i;
        for (i = 0; i <= RAND_MAX; i++)
        {
            return i;
        }

    return i;
}

int main()
{
    return 0;
}
